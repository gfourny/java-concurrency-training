baseUrl: http://localhost:${wiremock.server.port}

spring:
  datasource:
    hikari:
      maximum-pool-size: 5
      connection-timeout: 250
  sql:
    init:
      mode: always
#  threads:
#    virtual:
#      enabled: true